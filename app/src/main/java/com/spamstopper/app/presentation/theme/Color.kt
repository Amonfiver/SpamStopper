package com.spamstopper.app.presentation.theme

import androidx.compose.ui.graphics.Color

// Colores Primarios
val Primary = Color(0xFF2563EB)        // Azul confianza profesional
val PrimaryVariant = Color(0xFF1E40AF) // Azul oscuro autoridad
val Secondary = Color(0xFF10B981)      // Verde éxito/seguridad
val SecondaryVariant = Color(0xFF059669)

// Colores Funcionales
val Error = Color(0xFFEF4444)          // Rojo advertencia (spam)
val Warning = Color(0xFFF59E0B)        // Ámbar precaución
val Success = Color(0xFF10B981)        // Verde confirmación
val Info = Color(0xFF3B82F6)           // Azul información

// Neutrales (Light Mode)
val Background = Color(0xFFF9FAFB)     // Gris casi blanco
val Surface = Color(0xFFFFFFFF)        // Blanco puro
val SurfaceVariant = Color(0xFFF3F4F6)
val OnSurface = Color(0xFF111827)      // Gris oscuro texto
val OnSurfaceVariant = Color(0xFF6B7280)

// Neutrales (Dark Mode)
val BackgroundDark = Color(0xFF111827)
val SurfaceDark = Color(0xFF1F2937)
val SurfaceVariantDark = Color(0xFF374151)
val OnSurfaceDark = Color(0xFFF9FAFB)
val OnSurfaceVariantDark = Color(0xFF9CA3AF)

// Colores de Categorías (para iconos)
val CategorySales = Color(0xFFEF4444)      // Rojo
val CategoryInsurance = Color(0xFFF59E0B)  // Naranja
val CategoryLottery = Color(0xFFFBBF24)    // Amarillo
val CategoryUtilities = Color(0xFF3B82F6)  // Azul
val CategoryOther = Color(0xFF6B7280)      // Gris